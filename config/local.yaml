env: "local"

storage:
  type: "postgres" # memory или postgres

postgres:
  host: "postgres"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "url-shortener"
  sslmode: "disable"

http_server:
  address: "0.0.0.0:8080"
  timeout: 4s
  idle_timeout: 60s
  user: "myuser"
  password: "mypass"

